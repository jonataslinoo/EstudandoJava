<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rjgconfeccoes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.CadastroPedidoProdutoActivity" />
        <activity android:name=".ui.activity.CadastroProdutoActivity" />
        <activity android:name=".ui.activity.CadastroClienteActivity" />
        <activity android:name=".ui.activity.CadastroPedidoActivity" />
        <activity android:name=".ui.activity.CadastroUsuarioActivity" />
        <activity android:name=".ui.activity.DashboardActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity
            android:name=".ui.activity.SplashScreenActivity"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>